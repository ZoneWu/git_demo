A创建的文件

在github上创建一个新的项目，一个新的远程仓库

首次上传需要验证用户名和密码，之后不需要，因为系统记录了凭据

给远程仓库地址起一个别名：git remote add url 别名

直接push可推送到远程仓库就好了，在push分值的时候添加-u，之后的操作直接push就可以了


B clone到本地之后，就可以修改A创建的文件

B在A修改之后并未更新本地，就修改了，这回导致冲突，需要人为的修改冲突